package client;

import org.junit.jupiter.api.Test;

import java.io.IOException;
import java.net.Socket;

import static org.junit.jupiter.api.Assertions.*;

class ServerOutTest {
    @Test
    void validateMessageNullShouldBeNull() throws IOException {
        /*
        Socket serverSocket = new Socket();
        Client client = new Client();
        ServerOut socket = new ServerOut(serverSocket, client);
        String message = null;
        assertNull(socket.validateMessage(message));
        */
    }


}